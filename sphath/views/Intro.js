
/* 
    INTRO

    a short animation to introduce the game
    Chris suggested balls that spell out 'Sphath' bouncing into place
*/

function Intro() {
    

    this.render = function () {
        //animation loop stuff goes here

        requestAnimFrame(repaint);
    }

    // finish the intro by moving to start menu
    //currentState=gameState.StartMenu;

   
}
