/*
    BUTTONS

    you call this and it makes the button
    to your specifications
*/

function button(color, text, xarc, yarc) {
    this.color = color;
    this.text = text;
    this.xarc = xarc;
    this.yarc = yarc;

    
}
